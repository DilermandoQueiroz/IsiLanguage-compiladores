programa

numero a, b, c, d, e.

escreva("Programa Teste").
escreva ("Digite A").
leia (a).
escreva ("Digite B").
leia (b).

e := 10.50.

se (a<b) entao
{
    c := a + b.
} 
senao
{
    c := a - b.
}

escreva ("C e igual a ").
escreva (c).

d := c * a + b - e.

escreva ("D e igual a ").
escreva (d).

fimprog.
